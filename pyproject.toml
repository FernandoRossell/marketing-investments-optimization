[project]
name = "retail-marketing-mmm-uplift"
version = "0.1.0"
description = "Skeleton repo for retail marketing MMM and uplift modeling"
readme = "README.md"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
